 resources:
  jobs:
    quantified_self_etl_hourly:
      name: quantified_self_etl_hourly
      trigger:
        pause_status: UNPAUSED
        periodic:
          interval: 30
          unit: MINUTES
      tasks:
        - task_key: refresh_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.quantified_self_etl_pipeline.id}
